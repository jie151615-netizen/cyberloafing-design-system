**PageBadge** — a page/step number inside a thin circular ring, used in the corner of every technique spread.

```jsx
<PageBadge number={5} />
<PageBadge number={10} color="var(--coral)" size={52} />
```

Red or coral ring only. Keep it small and cornered — it's a locator, not a feature.
